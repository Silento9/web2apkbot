The Android template expects a Gradle wrapper (`gradlew`, `gradle/wrapper/*`) in this directory.
For a production repository, generate it with a matching Gradle 8.x distribution before deployment.
The Docker image installs Android SDK/build-tools but does not download a Gradle wrapper automatically.
